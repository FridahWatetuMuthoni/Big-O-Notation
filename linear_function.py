def linear_function(arr):
    # the loop takes O(n) runtime- as the data grows, the number of iterations on the loop grows with it
    for i in range(len(arr)-1):
        print(100 * 10000)  # O(1)


arr = [1, 2, 3, 4, 5, 6, 7]
linear_function(arr)
